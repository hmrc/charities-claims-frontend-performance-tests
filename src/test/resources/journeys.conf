# Copyright 2023 HM Revenue & Customs
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#     http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

# Configure here your journeys. A journey is a sequence of requests at a certain load.

journeys {

  # Example
  # Give a name to the journey.
  GASDS-top-up-journey = {

    # The description will appear in the test report. Use something meaningful
    description = "Submit Claim journey with GASDS Top-up/Regulator/CorporateTrustee"

    # The load is in journeys per second. Put here the load you are going to have at the peak.
    # There is no need to put a higher value at this point. Use prerftest.loadPercentage in application.conf instead
    load = 0.5

    # This points to a csv file with the data you need to inject in the journey. [More here](https://github.com/hmrc/performance-test-runner#step-4-configure-the-user-feeder)
    # feeder = data/example.csv

    # The parts your journey is made of. A part is made one or more requests.
    parts = [
      UI-GASDS-journey-1,Agent-UI-GASDS-journey-1
    ]
  }

  gas-journey = {

      # The description will appear in the test report. Use something meaningful
      description = "Gift Aid Schedule journey"

      # The load is in journeys per second. Put here the load you are going to have at the peak.
      # There is no need to put a higher value at this point. Use prerftest.loadPercentage in application.conf instead
      load = 0.07

      # This points to a csv file with the data you need to inject in the journey. [More here](https://github.com/hmrc/performance-test-runner#step-4-configure-the-user-feeder)
      # feeder = data/example.csv

      # The parts your journey is made of. A part is made one or more requests.
      parts = [
        gas-journey-1,gas-journey-2,gas-journey-3
      ]
    }

  oi-journey = {

      # The description will appear in the test report. Use something meaningful
      description = "Other Income Schedule journey"

      # The load is in journeys per second. Put here the load you are going to have at the peak.
      # There is no need to put a higher value at this point. Use prerftest.loadPercentage in application.conf instead
      load = 0.07

      # This points to a csv file with the data you need to inject in the journey. [More here](https://github.com/hmrc/performance-test-runner#step-4-configure-the-user-feeder)
      # feeder = data/example.csv

      # The parts your journey is made of. A part is made one or more requests.
      parts = [
        oi-journey-1,oi-journey-2,oi-journey-3
      ]
    }

  cb-journey = {

      # The description will appear in the test report. Use something meaningful
      description = "Community Buildings Schedule journey"

      # The load is in journeys per second. Put here the load you are going to have at the peak.
      # There is no need to put a higher value at this point. Use prerftest.loadPercentage in application.conf instead
      load = 0.07

      # This points to a csv file with the data you need to inject in the journey. [More here](https://github.com/hmrc/performance-test-runner#step-4-configure-the-user-feeder)
      # feeder = data/example.csv

      # The parts your journey is made of. A part is made one or more requests.
      parts = [
        cb-journey-1,cb-journey-2,cb-journey-3
      ]
    }

  cc-journey = {

      # The description will appear in the test report. Use something meaningful
      description = "Connected Charities Schedule journey"

      # The load is in journeys per second. Put here the load you are going to have at the peak.
      # There is no need to put a higher value at this point. Use prerftest.loadPercentage in application.conf instead
      load = 0.07

      # This points to a csv file with the data you need to inject in the journey. [More here](https://github.com/hmrc/performance-test-runner#step-4-configure-the-user-feeder)
      # feeder = data/example.csv

      # The parts your journey is made of. A part is made one or more requests.
      parts = [
        cc-journey-1,cc-journey-2,cc-journey-3
      ]
    }

  journey-all-schedules-org = {

      # The description will appear in the test report. Use something meaningful
      description = "End to End Organisation Journey with GASDS and all 4 schedules uploaded"

      # The load is in journeys per second. Put here the load you are going to have at the peak.
      # There is no need to put a higher value at this point. Use prerftest.loadPercentage in application.conf instead
      load = 0.089

      # This points to a csv file with the data you need to inject in the journey. [More here](https://github.com/hmrc/performance-test-runner#step-4-configure-the-user-feeder)
      # feeder = data/example.csv

      # The parts your journey is made of. A part is made one or more requests.
      parts = [
        schedule-journey-all-iterations
      ]
    }

  journey-all-schedules-agent = {

      # The description will appear in the test report. Use something meaningful
      description = "End to End Agent  Journey with GASDS and all 4 schedules uploaded"

      # The load is in journeys per second. Put here the load you are going to have at the peak.
      # There is no need to put a higher value at this point. Use prerftest.loadPercentage in application.conf instead
      load = 0.089

      # This points to a csv file with the data you need to inject in the journey. [More here](https://github.com/hmrc/performance-test-runner#step-4-configure-the-user-feeder)
      # feeder = data/example.csv

      # The parts your journey is made of. A part is made one or more requests.
      parts = [
        schedule-journey-all-iterations
      ]
    }


}


# Default behaviour is to run all journeys. If that is not what you need you can specify the list of journeys to run
journeysToRun = [
   journey-all-schedules-org
   journey-all-schedules-agent
   GASDS-top-up-journey
#   hello-world-3
]

# You can specify the same list of journeys via environment variables:
# journeysToRun.0 = hello-world-1
# journeysToRun.1 = hello-world-3
